# Inference engine selection (see src/services/inference/index.ts):
#   tfjs  (default) — real in-browser TensorFlow.js model (~125 MB download on the generator page)
#   mock            — instant demo engine for UI development; no model download, output is fake
#
# Copy this file to `.env.local` to override locally.
VITE_INFERENCE_ENGINE=tfjs
